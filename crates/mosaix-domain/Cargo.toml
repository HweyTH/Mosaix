[package]
name = "mosaix-domain"
description = "Core domain types: IDs, geometry primitives, state models, commands, and events."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }