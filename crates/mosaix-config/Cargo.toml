[package]
name = "mosaix-config"
description = "Configuration schema, validation, atomic reload, and migrations."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]