[package]
name = "mosaix-engine"
description = "Authoritative state reducer, reconciliation, placement diff, and transaction planning."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
mosaix-domain = { path = "../mosaix-domain" }
tracing = { workspace = true }
